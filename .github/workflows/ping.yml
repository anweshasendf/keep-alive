name: Keep Azure Container Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes UTC
  workflow_dispatch:          # manual run option

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Log timestamp
        run: echo "🔔 Pinging site at $(date -u)"

      - name: Curl the site
        run: curl -s https://apphub.danfoss.com/app/hose-end-fitting > /dev/null
